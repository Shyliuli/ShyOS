# generated by grok-4.5<noreply@x.ai> @ cursor
# trap：Rust 实现 + native trap entry

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
TRAP_S_OBJ := $(OUT_DIR)/trap.o

.PHONY: build clean cargo-check

ifeq ($(BACKEND),linux_user)
build:
else
build: $(TRAP_S_OBJ)
endif

cargo-check:
	$(CARGO) check --manifest-path Cargo.toml --target $(RUST_TARGET)

$(OUT_DIR):
	mkdir -p $@

$(TRAP_S_OBJ): trap.S trap.h $(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(AS) $(ASFLAGS) -c -o $@ $<

clean:
	rm -rf target
