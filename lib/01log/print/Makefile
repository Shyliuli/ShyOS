# generated by gpt-5.6-sol<noreply@openai.com> @ codex
# print：始终存在的 early stdio；Rust API 通过正常 rlib 依赖进入 Rust image

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
PRINT_C_OBJ := $(OUT_DIR)/print_c.o

.PHONY: build clean cargo-check

build: $(PRINT_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(PRINT_C_OBJ): early_stdio.c early_stdio.h \
		$(SHYOS_ROOT)/lib/00arch/uart/uart.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ early_stdio.c

cargo-check:
	$(CARGO) check --manifest-path Cargo.toml --target $(RUST_TARGET)

clean:
	rm -rf target
