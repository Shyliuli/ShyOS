# generated by grok-4.5<noreply@x.ai> @ pi
# print crate：Rust API/FFI 包装；native C 对象由 Makefile 统一进入 01log 层

[package]
name = "print"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
uart = { path = "../../00arch/uart" }
