# generated by gpt-5.6-sol<noreply@openai.com> @ codex
# log 层 Rust facade：early console 与 panic。

[package]
name = "shyos-log"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
panic = { path = "panic" }
print = { path = "print" }
shyos-arch = { path = "../00arch" }
shyos-stdio = { path = "../stdio" }
