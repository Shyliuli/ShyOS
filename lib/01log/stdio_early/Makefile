# generated by gpt-5<noreply@openai.com> @ codex

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
STDIO_EARLY_C_OBJ := $(OUT_DIR)/stdio_early_c.o

.PHONY: build clean cargo-check

build: $(STDIO_EARLY_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(STDIO_EARLY_C_OBJ): stdio_early.c \
		$(SHYOS_ROOT)/lib/stdio/stdio.h \
		$(SHYOS_ROOT)/lib/01log/print/early_stdio.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ stdio_early.c

cargo-check:
	$(CARGO) check --manifest-path Cargo.toml --target $(RUST_TARGET)

clean:
	rm -rf target
