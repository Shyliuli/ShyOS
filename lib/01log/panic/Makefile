# generated by deepseek-v4-pro<noreply@deepseek.com> @ pi
# panic：native 层只需要 C 实现；声明在 panic.h
# Rust API 通过正常 rlib 依赖进入 Rust image

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := target
PANIC_C_OBJ := $(OUT_DIR)/panic_c.o

.PHONY: build clean cargo-check

build: $(PANIC_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(PANIC_C_OBJ): panic.c panic.h \
		$(SHYOS_ROOT)/lib/01log/print/early_stdio.h \
		$(SHYOS_ROOT)/lib/00arch/shutdown/shutdown.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ panic.c

cargo-check:
	$(CARGO) check --manifest-path Cargo.toml --target $(RUST_TARGET)

clean:
	rm -rf $(OUT_DIR)
