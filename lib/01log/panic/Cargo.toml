# generated by deepseek-v4-pro<noreply@deepseek.com> @ pi
# panic crate：Rust 包装 C panic；native C 对象由 Makefile 进入 01log 层

[package]
name = "panic"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"
crate-type = ["rlib"]

[dependencies]
print = { path = "../print" }
shutdown = { path = "../../00arch/shutdown" }
