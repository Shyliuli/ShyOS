# generated by gpt-5<noreply@openai.com> @ codex
# allocator 稳定接口模块；具体实现由 provider 提供。

[package]
name = "shyos-alloc"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"
crate-type = ["rlib"]

[package.metadata.shyos]
capability = "alloc"
