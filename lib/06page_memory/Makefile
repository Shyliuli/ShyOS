# generated by kimi-k3<noreply@moonshot.cn> @ kimi-code
# 06page_memory 层：freelist pmm provider。

SHYOS_ROOT ?= $(abspath ../..)
include $(SHYOS_ROOT)/mk/config.mk

LAYER := 06page_memory
OUT_DIR := $(SHYOS_TARGET_DIR)

FREELIST_DIR := freelist
FREELIST_C_OBJ := $(FREELIST_DIR)/$(SHYOS_TARGET_DIR)/freelist_c.o

LAYER_ARCHIVE := $(OUT_DIR)/libshyos_$(LAYER).a
LAYER_RUST_LIB := shyos_$(LAYER)_rust

ifneq ($(filter SHYOS_PMM_FREELIST,$(SHYOS_DEFINES)),)
LAYER_OBJS := $(FREELIST_C_OBJ)
else
LAYER_OBJS :=
endif

.PHONY: clean

$(OUT_DIR):
	mkdir -p $@

$(LAYER_ARCHIVE): $(LAYER_OBJS) $(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	rm -f $@
	$(AR) rcs $@ $(LAYER_OBJS)

clean:
	rm -rf target
	$(MAKE) -C $(FREELIST_DIR) clean

include $(SHYOS_ROOT)/mk/layer.mk
