# generated by grok-4.5<noreply@x.ai> @ pi
# shutdown crate：
# Rust 包装 C 的 shutdown；C/Rust 对象由 Makefile 分别进入 00arch 层

[package]
name = "shutdown"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]
