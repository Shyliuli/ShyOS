# generated by grok-4.5<noreply@x.ai> @ cursor
# time：
#   - QEMU：Rust 实现（Cargo staticlib 闭包）
#   - linux_user：C 的 clock_gettime 适配进入 00arch archive

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
TIME_LINUX_C_OBJ := $(OUT_DIR)/time_linux_user_c.o

.PHONY: build clean

ifeq ($(BACKEND),linux_user)
TIME_BUILD_OBJS := $(TIME_LINUX_C_OBJ)
else
TIME_BUILD_OBJS :=
endif

build: $(TIME_BUILD_OBJS)

$(OUT_DIR):
	mkdir -p $@

$(TIME_LINUX_C_OBJ): time_linux_user.c time.h \
		$(SHYOS_ROOT)/lib/board/board.h \
		$(SHYOS_ROOT)/lib/board/linux_user/linux_user.h \
		$(SHYOS_ROOT)/lib/board/linux_user/syscall.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ $<

clean:
	rm -rf target
