# generated by grok-4.5<noreply@x.ai> @ pi
# 00arch/init：纯 C 启动钩子，无 Rust wrapper。

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
INIT_C_OBJ := $(OUT_DIR)/init_c.o

.PHONY: build clean

build: $(INIT_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(INIT_C_OBJ): init.c init.h \
		$(SHYOS_ROOT)/lib/alloc/alloc.h \
		$(SHYOS_ROOT)/lib/board/board.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ init.c

clean:
	rm -rf target
