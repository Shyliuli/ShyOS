# generated by gpt-5<noreply@openai.com> @ codex
# rand is implemented in C; Rust uses the wrapper crate.

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := target
RAND_C_OBJ := $(OUT_DIR)/rand_c.o

.PHONY: build clean cargo-check

build: $(RAND_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(RAND_C_OBJ): rand.c rand.h $(SHYOS_ROOT)/lib/board/board.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ rand.c

cargo-check:
	$(CARGO) check --manifest-path Cargo.toml --target $(RUST_TARGET)

clean:
	rm -rf $(OUT_DIR)
