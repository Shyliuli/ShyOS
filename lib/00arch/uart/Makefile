# generated by gpt-5<noreply@openai.com> @ codex
# arch/uart 组件：
#   - Rust 实现由所选 image layer 的 Cargo staticlib 闭包统一构建
#   - Linux user 的 syscall 适配仍作为 C 对象进入 00arch

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
UART_LINUX_C_OBJ := $(OUT_DIR)/linux_user_c.o

.PHONY: build clean

ifeq ($(BACKEND),linux_user)
UART_BUILD_OBJS := $(UART_LINUX_C_OBJ)
else
UART_BUILD_OBJS :=
endif

build: $(UART_BUILD_OBJS)

$(OUT_DIR):
	mkdir -p $@

$(UART_LINUX_C_OBJ): linux_user.c \
		$(SHYOS_ROOT)/lib/board/board.h \
		$(SHYOS_ROOT)/lib/board/linux_user/linux_user.h \
		$(SHYOS_ROOT)/lib/board/linux_user/syscall.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ $<

clean:
	rm -rf target
