# generated by gpt-5<noreply@openai.com> @ codex
# uart crate（00arch 层）：
# 1) Rust 世界：裸机目标 path 依赖 board，对外提供 Rust API
# 2) C 世界：#[no_mangle] extern "C"，由 image layer staticlib 闭包导出

[package]
name = "uart"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]

[target.'cfg(not(target_os = "linux"))'.dependencies]
board = { path = "../../board" }
