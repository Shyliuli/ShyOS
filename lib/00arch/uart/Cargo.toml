# generated by gpt-5<noreply@openai.com> @ codex
# uart crate（00arch 层）：始终提供 early 轮询通道，并声明可选 raw UART capability。

[package]
name = "uart"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]

[target.'cfg(not(target_os = "linux"))'.dependencies]
board = { path = "../../board" }

[package.metadata.shyos]
capability = "uart"

[[package.metadata.shyos.provides]]
capability = "uart"
define = "SHYOS_UART_POLLING"
