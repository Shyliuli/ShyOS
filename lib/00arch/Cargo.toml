# generated by gpt-5.6-sol<noreply@openai.com> @ codex
# 00arch 层 Rust facade：只表达依赖和重新导出

[package]
name = "shyos-arch"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
board = { path = "../board" }
irq = { path = "irq" }
shyos-atomic = { path = "atomic" }
shyos-rand = { path = "rand" }
shutdown = { path = "shutdown" }
uart = { path = "uart" }
shyos-time = { path = "time" }
