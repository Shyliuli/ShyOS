# generated by gpt-5<noreply@openai.com> @ codex
SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
RINGBUFFER_C_OBJ := $(OUT_DIR)/ringbuffer_c.o

.PHONY: build clean

build: $(RINGBUFFER_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(RINGBUFFER_C_OBJ): ringbuffer.c ringbuffer.h \
		$(SHYOS_ROOT)/lib/04data_structure/deque/deque.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ ringbuffer.c

clean:
	rm -rf target
