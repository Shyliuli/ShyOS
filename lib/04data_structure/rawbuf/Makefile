# generated by gpt-5<noreply@openai.com> @ codex
SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := target
RAWBUF_C_OBJ := $(OUT_DIR)/rawbuf_c.o

.PHONY: build clean

build: $(RAWBUF_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(RAWBUF_C_OBJ): rawbuf.c rawbuf.h \
		$(SHYOS_ROOT)/lib/03basic/result/result.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ rawbuf.c

clean:
	rm -rf $(OUT_DIR)
