# generated by deepseek-v4-pro<noreply@deepseek.com> @ pi
# simple_alloc C 对象 + 宿主测试

SHYOS_ROOT ?= $(abspath ../../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)
SIMPLE_ALLOC_C_OBJ := $(OUT_DIR)/simple_alloc_c.o

.PHONY: build clean

build: $(SIMPLE_ALLOC_C_OBJ)

$(OUT_DIR):
	mkdir -p $@

$(SIMPLE_ALLOC_C_OBJ): simple_alloc.c simple_alloc.h \
		$(SHYOS_ROOT)/lib/alloc/alloc.h \
		$(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ simple_alloc.c

clean:
	rm -rf target
