# C-only 05timer image 的完整 Rust staticlib 闭包。

[package]
name = "shyos-05timer-rust"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"
crate-type = ["staticlib"]

[dependencies]
shyos_05timer = { path = ".." }
