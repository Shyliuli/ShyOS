# generated by grok-4.5<noreply@x.ai> @ pi
# board crate：板级常量 / 条件编译
# 可被其他 Rust 组件 path 依赖；也可由 Makefile 编成 board.o 给 C 世界链接

[package]
name = "board"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]
