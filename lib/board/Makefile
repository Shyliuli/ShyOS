# generated by gpt-5<noreply@openai.com> @ codex
# board 的 Rust 实现由所选 image layer 的 Cargo staticlib 闭包统一构建。

SHYOS_ROOT ?= $(abspath ../..)
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := $(SHYOS_TARGET_DIR)

.PHONY: build clean

build:

clean:
	rm -rf target
