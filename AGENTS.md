## 项目背景 Spec（模型行为约束）

### 背景

ShyOS 是一个面向 `riscv64gc-unknown-none-elf` 的分层裸机库（`no_std`），不是一个
传统的单体 kernel。仓库由 Make（native 链接、镜像产出）与 Cargo workspace（Rust
crate 依赖）共同构建：

- `lib/` 下是编号层
  （`00arch -> 01log -> 02core -> 03basic -> 04data_structure`）与共享模块
  （如 `board`）；
- `app/` 与 `kernel/` 是链接某一层的最终 image 示例；
- `tools/layer_graph.py` 负责模块发现、DAG 检查与 `.layer.ld` 闭包计算；
- `config/shyos.mk` 是完整 image 配置模板；每个 app、kernel、test 在自己的目录中
  保存一份 `shyos.mk`，独立选择 backend、provider 和最高链接层；
- `test/` 存放以 Linux user 为主、少量 QEMU 为辅的分层 image 测试。

项目的核心价值是"层即接口"：APP 链接哪一层，就获得哪一层的能力；全层链接后，
APP 只需少量修改即可成为 Linux 二进制兼容的 kernel。任何改动都不得破坏这一点。

### 模型必须遵守的约束

1. 不破坏分层：新代码必须落在正确的层或共享模块内，依赖只能指向更低编号层；
   不确定归属时先提问，而不是随手放置。
2. 不引入 `std`、libc 或外部 crate 依赖，除非用户明确同意；所有代码默认
   `no_std`、freestanding。
3. 不绕过构建体系：不得在 app/kernel 手工枚举对象文件、不得在叶子组件私加
   `build.rs` 或链接脚本；构建顺序交给 `layer.toml` + `tools/layer_graph.py`。
4. 双世界接口是硬要求：每个组件必须同时可被 C（稳定 ABI + 头文件）与 Rust
   （Cargo path 依赖）使用，细节见下文各节。
5. runtime 类模块（内存管理、task、syscall 等）必须可被 APP 替换或注册扩展
   （weak link、config 编译、注册表机制），不得写死策略。
6. 实现 `AI impl` 注释标记的函数后，必须按下文格式改写为 generated-by 注释；
   不清楚自己的模型名或工具名时必须询问，不得编造。
7. 改动后应通过 `make` 与 `test/` 下相关测试验证；涉及模块图的改动需确认
   `tools/layer_graph.py` 检查仍然通过。
8. 保持文档同步：改变层结构、DAG 或构建约定时，必须同步更新本文件对应章节。

## Spec

### 项目背景

ShyOS 是我个人的实验项目：一个分层的裸机 lib。

设计核心是用「分层 + DAG」换取清晰度：

- 层间依赖只允许从高层指向低层，禁止反向和横向；
- 层内组件依赖深度不超过 3；
- APP 链接到哪一层，就获得哪一级别的能力——最小启动、日志、
  核心设施……全层链接即是一个 OS（目标是 Linux 二进制兼容）。

层就是能力级别，链接就是能力选择。任何改动不得模糊这条主线。

### 模型行为约束

这是我的项目，我需要对它的每一行代码有掌控感。AI 只是我双手的延伸：

1. 忠实于我的描述和伪代码实现，不做我没有要求的事——禁止自行添加
   fallback、防御性分支、兼容垫片、"顺手"的重构或抽象。
2. 一切从简：实现我说的最小版本。想到"更好的做法"可以在回复里提，
   但代码里只写我要求的。
3. 我的描述有歧义或伪代码有明显问题时，先问我，不要替我决定。
4. 遵守本文件后续章节的工程约定（分层、双世界接口、构建体系）；
   与我的明确指示冲突时，以我的指示为准，但要指出冲突。

## 项目介绍
本项目是一个分层裸机lib.APP可以选择链接不同层获得从最小化启动，到Linux兼容(二进制程序兼容)OS的能力.
由于项目特性，在加入任何模块时，需注意:
1. 这是否是一个库提供的模块？
2. 如果这是为了实现某个能力的runtime(如fork需要内存管理，task等)，是否足够灵活，允许:
  1. APP若是一个kernel,允许它通过注册等机制修改本模块(如syscall表)
  2. 该模块是否应该通过weak link或者config编译，允许APP提供替换方案(如调度器)
3. 目标:像库一样分层，清晰，同时对于全层链接，app仅需简单修改即可获得一个linux兼容kernel
4. 每一个模块都必须有rust和c接口，且接口统一(类c风格，但rust侧要做raii等简单包装). 参考string_no_alloc的rust侧设计.
## AI impl
你应该搜索AI impl进行实现
实现后将注释改为AIGC
例如:
```
//AI impl fn open(fd)->int
//打开一个文件
//调用vfs
```
你应该完成这一函数实现，并将注释改为
```
//generated by model_name<noreply@domain.com> @ tool_name
//fn open(fd)->int
//打开一个文件
//调用vfs
```

其中示例：
```
//generated by gpt-5.6-sol<noreply@openai.com> @ codex
//generated by grok-4.5<noreply@x.ai> @ grok-build
//generated by deepseek-v4-pro<noreply@deepseek.com> @ pi
```

如果因为各种原因导致你不清楚当前环境或者自己是什么模型，请询问.

## C / Rust 双世界组件组织

每个同时服务 C 和 Rust 的组件都应形成一个可通过 path 依赖使用的 Rust
crate（`rlib`）。C/汇编实现由组件 Makefile 生成 native 对象；Rust 实现进入 Cargo
管理的 image staticlib 闭包。

根据实现所在语言选择以下组织方式：

1. Rust 实现、向 C 暴露接口
   - Rust crate 正常提供 Rust API，其他 Rust crate 通过 Cargo path 依赖调用。
   - 需要给 C 使用的入口通过 `extern "C"` 和稳定符号名导出，并提供对应头文件。
   - 不为单个组件使用 `cargo rustc --emit=obj`。Rust 实现由最终 Rust image
     staticlib，或 C-only image 所选最高层的 Rust staticlib 闭包统一提供。

2. C 实现、向 Rust 暴露接口
   - C 源文件和头文件定义实现及 C ABI。
   - 组件 Makefile 把 C 源文件编译为 `<name>_c.o`。
   - Rust crate 使用 FFI 提供最小、安全边界清晰的 Rust 包装。
   - Rust wrapper 只进入该 crate 的 `rlib`，不单独塞入 native 层 archive。
   - 叶子 crate 默认不使用 `build.rs` 编译或链接 C；最终 native 链接由层级构建统一
     完成，避免 Cargo 与 Make 重复维护 C 编译逻辑。

3. 其他复杂情况
   - 可以混合 C、Rust 和汇编实现，但最终必须同时支持：
     - C 世界通过稳定的 C ABI 和头文件使用；
     - Rust 世界通过正常的 Cargo path 依赖使用。
   - 不允许要求调用方手工重复配置同一组件的内部对象文件。

组件目录通常应自行包含 `Cargo.toml`、Rust 源文件、Makefile，以及按需提供的
C/汇编源文件和公开头文件。只有需要脱离 ShyOS Make 构建、由 Cargo 独立生成最终
可执行文件时，才允许在最终 app/kernel crate 使用一个集中式 `build.rs`；不要在每个
叶子组件重复放置链接脚本。

## 编号层与依赖 DAG

所有层都直接位于 `lib/` 下，目录名必须使用两位十进制编号加层名：

```text
00arch
01log
02core
03basic
04data_structure
```

每层必须提供 `layer.toml`，在 `[layer].dependencies` 中声明直接下层。编号层外允许
存在 `lib/board`、`lib/alloc`、`lib/stdio` 这类无编号共享基础或能力接口模块；共享模块不是
image layer，不生成 `.layer.ld`。

构建前使用 `tools/layer_graph.py` 自动发现全部模块。模块节点由含 `Cargo.toml` 或
Makefile 的目录确定，边由 Cargo path 依赖和 C 本地头文件引用推导。构建器必须对
模块图执行拓扑排序，并执行以下检查：

C 编译的模块头文件搜索路径也必须由同一模块发现结果自动生成；除仓库根
`include/` 外，不在 `mk/config.mk` 中手工枚举模块目录。

- 依赖必须指向更小编号的层；
- 不同层使用相同编号时，禁止它们之间产生横向依赖；
- 同一编号层内的组件模块依赖允许存在，但最长依赖链不得超过 3 个组件节点；
  facade 层节点不计入深度。模块图仍必须无环，并按拓扑序构建；
- 共享模块可以被编号层依赖，但不得反向依赖任何编号层模块；
- 可替换 runtime 使用共享能力模块表达稳定依赖，具体实现通过
  `package.metadata.shyos.provides` 声明能力名和 config define；消费者不得直接依赖
  某个 provider；
- 跨层模块引用不得绕过 `layer.toml` 的传递依赖闭包；
- 检查失败时必须在编译任何本层对象前拒绝构建。

层 Makefile 不应手写层内模块构建顺序；公共 `mk/layer.mk` 必须使用模块 DAG 的
拓扑结果构建模块。需要独立复用但不构成 image 层级的最底层能力，可以像 board
一样作为共享模块；共享模块必须保持无编号层反向依赖。

能力接口模块在 `[package.metadata.shyos]` 中声明 `capability`。provider 使用
`[[package.metadata.shyos.provides]]` 声明 `capability` 与选择它的 `define`。
`tools/layer_graph.py` 校验 provider 指向已存在的能力；`make dag-html` 根据当前
`SHYOS_DEFINES` 标记已选择 provider。provider 关系用于能力展示和配置闭包，不替代
普通模块依赖边。

`early_stdio` 是 `01log/print` 始终提供的启动与 panic 通道。正式 `stdio` 使用
`lib/stdio` capability，配置必须选择恰好一个 `SHYOS_STDIO_*` provider。当前
`SHYOS_STDIO_EARLY` 同时提供 C 的正式 stdio 符号和 Rust 的 `print!`、`println!`；
`early_*` 接口不受该选择影响。

当前 DAG 为：

```text
00arch -> 01log -> 02core -> 03basic -> 04data_structure
```

箭头表示从低层能力到高层能力；依赖方向与箭头相反，例如
`04data_structure` 依赖 `03basic`。

## Native 层级链接

每一层必须提供以下 native 产物：

1. `libshyos_<layer>.a`
   - 静态 archive，只收集本层自己的 C、汇编对象。
   - 不把下层 archive 物理合并进本层 archive。
   - Rust 实现、FFI wrapper、trait 和 facade 不作为独立对象重复归档。

2. `libshyos_<layer>_rust.a`
   - 由该层的 `rust_image` staticlib 壳通过 Cargo 构建。
   - 依赖本层 facade，包含从当前层到最底层的完整 Rust crate、`core`、`alloc` 与
     compiler builtins 闭包。
   - 仅供没有自身 Rust staticlib 的 C-only image 使用；其中的 panic handler 属于该
     合成 image root，不属于 facade crate。

3. `<layer>.layer.ld`
   - 文本链接接口，使用 `GROUP(...)` 按“当前层到最底层”的顺序列出传递 archive。
   - 包含各层 C/汇编 archive，以及所选最高层唯一的
     `libshyos_<layer>_rust.a`。
   - 不包含下层的 Rust staticlib，因为最高层 Rust staticlib 已包含完整闭包。
   - app/kernel 只链接选择的最高层 `.layer.ld`，不得再手工枚举 `init.o` 或各组件
     对象。

4. `<layer>.norust.layer.ld`
   - 只包含当前层到最底层的 C/汇编 archive。
   - 供已经声明 `IMAGE_RUST_STATICLIB` 的 Rust app/kernel 使用，避免再次链接层 Rust
     闭包。

因此 `04data_structure.layer.ld` 包含
`04data_structure -> 03basic -> 02core -> 01log -> 00arch` 的 C/汇编 archive，
再包含唯一的 `libshyos_04data_structure_rust.a`。`init.o` 属于 `00arch`。
board 与 uart 的 Rust 实现只由 Cargo staticlib 闭包构建，不再生成
`board_rs.o` 或 `uart.o`。

当前五层都传递包含 `00arch` 的启动入口，可以作为 app/kernel 的最终 image layer。

为控制镜像体积，C 使用 `-ffunction-sections -fdata-sections`，最终链接使用
`--gc-sections`。除注册表、启动表等确实要求全部保留的内容外，不使用
`--whole-archive`；必须保留的 section 在主链接脚本中用 `KEEP()`。

## Rust 层级接口

Rust 不使用 `.layer.ld` 表达 crate 依赖。每层提供一个轻量 facade crate：

- `shyos-arch` 位于 `00arch`，重新导出 board、irq、rand、uart、shutdown。
- `shyos-log` 位于 `01log`，重新导出 shyos-arch、print、panic。
- `shyos-core` 位于 `02core`，重新导出 shyos-log、error、string_no_alloc。
- `shyos-basic` 位于 `03basic`，重新导出 shyos-core、obj、result、alloc。
- `shyos-data-structure` 位于 `04data_structure`，重新导出 shyos-basic、vec、
  string。

Rust app 只 path 依赖所需的最高层 facade crate。Cargo 负责编译期依赖和去重，Make
仍负责最终 ELF 链接。

Rust app/kernel 不应只用 `--emit=obj` 输出顶层 crate，因为该对象不包含依赖 crate、
`core` 和 compiler builtins。应让最终 Rust image crate 生成 `staticlib`，由 Cargo 将
完整 Rust 依赖闭包装进 app/kernel archive，再由 Make 将该 archive 与选择的 native
`.norust.layer.ld` 一起链接。image crate 负责 panic handler 等最终运行时定义，
facade crate 不得定义这些全局运行时入口。

C-only image 没有自身 Rust staticlib，使用普通 `.layer.ld`，从所选最高层的
`rust_image` staticlib 壳获得完整 Rust 闭包。所有 Cargo 构建统一使用根目录
`target/cargo`、相同 target/profile/RUSTFLAGS 与 build-std 参数，使层 staticlib 和
app staticlib 复用同一批依赖 crate 缓存。

`app/` 使用子项目目录组织，根 Makefile 的 `APP_NAME` 选择
`app/<APP_NAME>/`。每个 app 子项目自行提供 Makefile、C 入口和按需提供的 Rust
	`staticlib` image crate，并保存一份从 `config/shyos.mk` 复制的本地 `shyos.mk`。
	本地配置通过 `IMAGE_LAYER` 选择最高链接层，并独立选择 backend 与 capability
	provider；`CONFIG=DEBUG/RELEASE` 同时选择 C 优化与调试参数和 Cargo profile。
	kernel 和每个 test image 遵循相同约定。

最终 image 通过公共 `mk/image.mk` 链接自身输入。声明
`IMAGE_RUST_STATICLIB` 时选择 `.norust.layer.ld`，否则选择普通 `.layer.ld`。
所有 image 输入和层链接接口放在同一个 `--start-group/--end-group` 中解决 archive
之间的循环引用，不得在 image 内手工枚举层对象。根 Makefile 只递归调度
app/kernel/test，
不预先加载某个 image 的配置，因此 `make all` 中 app 与 kernel 可以使用不同配置；
共享层产物按 image 顺序重建，不允许两个不同配置的最终 image 并行构建。

`tools/new_app.py` 创建可被 Cargo workspace 和 rust-analyzer 自动发现的混合 C/Rust
app 模板；根入口为 `make new-app APP=app/<name>`。

Rust 工具链使用 nightly，并安装 `rust-src` 与 `rust-analyzer`。最终 Rust image crate
必须持续使用
`-Z build-std=core,alloc,compiler_builtins`，让 `core`、`alloc` 和 compiler builtins
按当前 target 与 panic 策略重新构建；不得通过手写 `rust_eh_personality` 等占位符
掩盖 hosted sysroot 与 `no_std` image 运行时闭包不一致的问题。
