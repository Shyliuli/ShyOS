# generated by gpt-5.6-sol<noreply@openai.com> @ codex
# Target and cfg selection come from config/shyos.mk through mk/config.mk.
# Keeping Cargo's global config backend-neutral prevents direct Cargo settings
# from disagreeing with the selected native backend.

[build]
