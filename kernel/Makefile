# generated by grok-4.5<noreply@x.ai> @ pi
# 生成 kernel.o（含 main）；flags 来自 mk/config.mk / 根 export

SHYOS_ROOT ?= $(abspath ..)
SHYOS_CONFIG ?= $(CURDIR)/shyos.mk
include $(SHYOS_ROOT)/mk/config.mk

OUT_DIR := target
KERN_OBJ := $(OUT_DIR)/kernel.o

$(OUT_DIR):
	mkdir -p $@

$(KERN_OBJ): main.c $(SHYOS_CONFIG_STATE) | $(OUT_DIR)
	$(CC) $(CFLAGS) -c -o $@ $<

IMAGE_NAME := kernel
IMAGE_INPUTS := $(KERN_OBJ)
IMAGE_EMIT_BIN := 1

include $(SHYOS_ROOT)/mk/image.mk
